<?xml version="1.0" encoding="UTF-8"?>

<odoo>

    <record id="view_hospital_management_patient_form" model="ir.ui.view">
        <field name="name">hospital.patient.form</field>
        <field name="model">hospital.management.patient</field>
        <field name="arch" type="xml">
            <form>
                <sheet>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="dob"/>
                            <field name="age"/>
                        </group>
                        <group>
                            <field name="gender"/>
                            <field name="ref"/>
                            <field name="active"/>
                        </group>
                    </group>
                </sheet>
                <notebook>
                    <page string="Appointments">
                        <field name="appointments">
                            <tree>
                                <field name="ref"/>
                                <field name="appointment_time" string="Appointment Time"/>
                                <field name="booking_date" string="Appointment Date"/>
                            </tree>
                        </field>
                    </page>
                </notebook>
                <!-- <div class="oe_chatter">
                    <field name="message_follower_ids" groups="base.group_user"/>
                    <field name="activity_ids"/>
                    <field name="message_ids"/>
                </div> -->
            </form>
        </field>
    </record>

    <record id="view_hospital_management_patient_tree" model="ir.ui.view">
        <field name="name">hospital.patient.tree</field>
        <field name="model">hospital.management.patient</field>
        <field name="arch" type="xml">
            <tree>
                <field name="name"/>
                <field name="ref"/>
                <field name="age"/>
                <field name="gender"/>
            </tree>
        </field>
    </record>

    <record id="view_hospital_management_patient_search" model="ir.ui.view">
        <field name="name">hospital.patient.search</field>
        <field name="model">hospital.management.patient</field>
        <field name="arch" type="xml">
            <search>
                <field name="name"/>
                <field name="gender"/>
                <filter name="archieve_filter" string="Archieved" domain="[('active', '=', False)]"/>
            </search>

            
            
        </field>
    </record>




    <record id="action_hospital_patient" model="ir.actions.act_window">
        <field name="name">Patients</field>
        <field name="res_model">hospital.management.patient</field>
        <field name="view_mode">tree,form</field>
        <field name="context">{}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create you first Patient
            </p>
        </field>
    </record>
</odoo>